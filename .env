# Local dev: SQLite (works without any external service, no signup needed)
# Production (Render): replace with your Neon PostgreSQL connection string
DATABASE_URL="file:./dev.db"
JWT_SECRET="change-this-to-a-long-random-string-in-production"
PORT=4000
